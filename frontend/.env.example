# In production: optional if using vercel.json rewrite (proxies /api -> Render)
# Local dev: leave empty (Vite proxy handles /api -> localhost:3000)
# Set this only if you want to call the backend directly (e.g., Render URL)
VITE_API_URL=https://getmyats.onrender.com
